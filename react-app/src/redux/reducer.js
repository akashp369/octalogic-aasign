const initialState={
    islaoding:false,
    iserror:false,
    isauth:false,
    data:null,
}

const appReducer=(state=initialState, action)=>{
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}

export default appReducer;